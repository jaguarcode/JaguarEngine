@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Required dependencies
find_dependency(Eigen3 3.4)

# Optional dependencies
if(@GDAL_FOUND@)
    find_dependency(GDAL 3.0)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/JaguarTargets.cmake")

check_required_components(Jaguar)
