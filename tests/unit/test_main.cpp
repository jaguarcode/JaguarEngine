/**
 * @file test_main.cpp
 * @brief Unit test entry point
 */

#include <gtest/gtest.h>

// Unit tests will be added here

TEST(CoreTest, BasicTypes) {
    // Test basic type sizes
    EXPECT_EQ(sizeof(double), 8);
}

TEST(CoreTest, Vec3Operations) {
    // Placeholder for Vec3 tests
    EXPECT_TRUE(true);
}

TEST(CoreTest, QuaternionOperations) {
    // Placeholder for Quaternion tests
    EXPECT_TRUE(true);
}
